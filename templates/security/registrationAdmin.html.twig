{% extends 'baseVisitor.html.twig' %}

{% block title %}Page d'inscription !{% endblock %}

{% block body %}
    {{ form_start(form)}}
    {{ form_widget(form)}}
    <button type="submit" class="btn btn-success">Inscriptio !</button>
    {{ form_end(form)}}
{% endblock %}