{% extends 'baseVisitor.html.twig' %}

{% block title %}Page de connexion à l'espace membre{% endblock %}

{% block body %}
<div class="loginMember">
    <h1 class="text-center">Connectez-vous à votre espace membre grâce au formulaire ci-dessous !</h1>
    <div class="formLoginMember">
        <form action="{{ path('security_login_members') }}" method="post">
                <label id="form_errorLoginMember" class="strong"></label>
            <div class="form-group">
                <input id="emailMemberLoginEmailType" placeholder="Adresse mail ..." required name="_username" type="text" class="form-control">
            </div>
            <div class="form-group">
                <input id="passwordMemberLoginPasswordType"  placeholder="Mot de passe ..." required name="_password" type="password" class="form-control">
            </div>
            <div class="form-group">
                <button type="submit" class="btn btn-success">Connexion !</button>
            </div>
        </form>
    </div>
</div>
{% endblock %}